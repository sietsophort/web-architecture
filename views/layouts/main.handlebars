<!DOCTYPE html>
<html>

<head>
  <title>Web-Arch</title>
</head>

<body>
  {{!-- {{>header}}  --}}
  {{{body}}}
  {{!-- {{>footer}}
    {{>additem}}
    {{>script}} --}}
  {{>script}}
</body>

</html>